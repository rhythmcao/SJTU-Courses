LetExp(
 DecList(
  VarDec(s,
   StringExp(Hello World!),
   true),
  DecList()),
 SeqExp(
  ExpList(
   CallExp(print,
    ExpList(
     varExp(
      SimpleVar(s)))),
   ExpList(
    CallExp(print,
     ExpList(
      StringExp(
)))))))