LetExp(
 DecList(
  FunctionDec(fact
   Fieldlist(
    n
    int,
    true,
    Fieldlist()),
   int
   IfExp(
    OpExp(
     EQ,
     varExp(
      SimpleVar(n)),
     IntExp(0)),
    IntExp(1),
    OpExp(
     MUL,
     varExp(
      SimpleVar(n)),
     CallExp(fact,
      ExpList(
       OpExp(
        MINUS,
        varExp(
         SimpleVar(n)),
        IntExp(1)))))),
   FunctionDec()),
  DecList()),
 SeqExp(
  ExpList(
   CallExp(fact,
    ExpList(
     IntExp(10))))))